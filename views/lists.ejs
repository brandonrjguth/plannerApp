<html>

<head>
    <link type="text/css" href="css/styles.css" rel="stylesheet">
</head>

<body>

   

    <% for (i=0; i<lists.length; i++){%>
        <div>
        <a href=/index/<%=wtf%>/<%=encodeURI(lists[i].name)%>><%=lists[i].name%></a>
        <form id="<%=lists[i].name%>" method="post" action="/lists/delete/<%=lists[i].name%>">
            <input type="hidden">
        </form>
        <button class='btn btn-primary' onclick='document.getElementById("<%=lists[i].name%>").submit()'>Delete</button>
           
    </div>
        
        <%
    };%>

    Add a List

    

    <form action=/lists method="POST">
        <input type="text" name="name">
        <button>
            Submit!
        </button>
    </form>


</body>


</html>